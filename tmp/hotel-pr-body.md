Replace generated hotel recommendations with Google Places lodging results, restrict nearby searches, and load property-linked photos automatically with attribution. Keep prices and ratings unavailable when not supplied, and enforce persistent request allowances with no unmetered Google retries.

Validation: 20 targeted backend tests passed; TypeScript and changed-component ESLint checks passed. One live search and photo lookup succeeded. Azure already has the matching server-side Google API key.
